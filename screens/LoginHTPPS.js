import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const LoginHTPPS = () => {
  return (
    <SafeAreaView>
      <Text>LoginHTPPS</Text>
    </SafeAreaView>
  );
};

export default LoginHTPPS;

const styles = StyleSheet.create({});
